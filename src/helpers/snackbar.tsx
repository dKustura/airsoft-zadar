import { OptionsObject } from 'notistack';

export const successNotification: OptionsObject = {
  variant: 'success',
};

export const errorNotification: OptionsObject = {
  variant: 'error',
};

export const warningNotification: OptionsObject = {
  variant: 'warning',
};

export const infoNotification: OptionsObject = {
  variant: 'info',
};
